# Race Locked

WoW Classic addon for Race Locked event rules and cross-guild race-grid data sharing.

## Tests

Run the Lua regression tests from the repo root:

```sh
npm test
```

The test script uses `luajit` and currently covers the RaceLockedDataBus channel join timing.
