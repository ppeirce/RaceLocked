{
  "scripts": {
    "prettier": "prettier --write .",
    "test": "npm run test:lua",
    "test:lua": "luajit tests/comms_channel_connection_spec.lua",
    "zip": "rsync -av --exclude='.*' Libs Functions Display Textures RaceLocked.lua RaceLocked.toc ToBeZipped/RaceLocked && cd ToBeZipped && zip -r RaceLocked.zip RaceLocked  -x '*.DS_Store'"
  },
  "devDependencies": {
    "@prettier/plugin-lua": "^0.0.3",
    "prettier": "^3.4.2"
  }
}
